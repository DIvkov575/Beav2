project_id: ...
region: northamerica-northeast1
bq_location: "us"

sources:
  pubsub_in:
    type: gcp_pubsub
    project: "..."
    subscription: "input-sub-1"
    decoding:
      codec: "json"
transforms:
  transform1:
    type: remap
    inputs:
      - pubsub-in
